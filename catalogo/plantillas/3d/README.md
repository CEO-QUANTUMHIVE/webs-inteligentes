# Plantillas 3D

Placeholder. Nivel de plantilla con elementos 3D integrados (más allá de las
"básicas" en CSS puro de `catalogo/plantillas/basicas/`) — todavía no existe
ninguna. Ver la skill `web-3d` en `.claude/skills/` para generar contenido 3D.

Cuando se construya la primera plantilla acá, seguir el mismo formato de
`ficha.json` + `README.md` que usan las plantillas básicas, sumando los
campos que haga falta para las dependencias 3D (por ejemplo, versión de
`three`).
