# Plantillas Scroll

Placeholder. Nivel de plantilla con narrativa anclada al scroll (en la línea
de `plantillas/concreto`, que ya usa GSAP para esto) — todavía no existe
ninguna dentro de `catalogo/`.

Cuando se construya la primera, seguir el mismo formato de `ficha.json` +
`README.md` que usan las plantillas básicas, documentando además la
librería de scroll que se use (GSAP ScrollTrigger u otra) en el campo
`dependencias`.
