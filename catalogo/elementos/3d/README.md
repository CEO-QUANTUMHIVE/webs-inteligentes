# Elementos 3D

Placeholder. Componentes visuales 3D individuales del catálogo — todavía no
se cargó ningún elemento acá. Ver la skill `web-3d` en `.claude/skills/` si
se necesita generar contenido 3D para poblar esta carpeta.

Cuando se agregue el primer elemento, documentar el formato de ficha que use
(análogo a `catalogo/plantillas/basicas/<id>/ficha.json`) en este README.
