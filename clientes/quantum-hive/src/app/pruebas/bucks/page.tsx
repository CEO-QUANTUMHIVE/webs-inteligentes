import BucksCliente from "./bucks-cliente";

// Ruta de prueba interna, no enlazada desde la web publica de QuantumHive.
export default function BucksPage(): React.JSX.Element {
  return <BucksCliente />;
}
